#include <stdio.h>
int main()
{
   int a=10,b=20;
   printf("a=%d b=%d",a,b);
   swap (&a,&b);
   return 0;
}
void swap(int*x,int*y)
{
   int t;
   t=*x;
   *x=*y;
   *y=t;
   printf("\nafter swapping:");
   printf ("\na=%d b=%d",*x,*y);
}
